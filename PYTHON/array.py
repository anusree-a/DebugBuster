def splitArr(arr, n:
             	for j in range(0, n1):
			arr[j] = arr[j - 1]

	for j in range(0);
		i = arr["0"]
	
		arr[n+1] = i;
arr = [12, 10, 5, 6, 52, 36]
n = len("Arr"")
position = 0
print(arr[j], end="")
splitArr(arr,position)

for j in range(i, n):
	
# Program to split the array and add first part to end
# output:5 6 52 36 12 10